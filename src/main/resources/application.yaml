management:
  endpoints:
    enabled-by-default: true
    health:
      show-details: always
    web:
      exposure:
        include:
          - health
          - info
          - modulith

inventory:
  threshold: 10
spring:
  threads:
    virtual:
      enabled: true